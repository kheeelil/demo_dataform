config {
  type: "assertion",
  description: "Vérifie qu'il n'y a aucun doublon de customer_id dans la table staging"
}

SELECT
  customer_id,
  COUNT(*) as nombre_de_doublons
FROM ${ref("stg_customers")}
GROUP BY 1
HAVING nombre_de_doublons > 1

